# Railway/Nixpacks configuration for Rails
[phases.setup]
nixPkgs = ["ruby_3_2", "postgresql", "nodejs-18_x"]

[phases.install]
cmds = ["bundle install"]

[start]
cmd = "bundle exec rails db:migrate && bundle exec puma -C config/puma.rb"
